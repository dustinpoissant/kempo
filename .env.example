DATABASE_URL=postgresql://kempo:kempo_dev_password@localhost:5433/kempo
BETTER_AUTH_SECRET=your_secret_here_generate_with_openssl_rand_hex_32
BETTER_AUTH_URL=http://localhost:3000

# Email Configuration
# For local development, choose between:
# - "mailpit" (testing only, emails captured in web UI at http://localhost:8025)
# - "smtp" (real emails sent through Hostinger)
EMAIL_MODE=mailpit

# SMTP Settings (required for production and EMAIL_MODE=smtp)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@yourdomain.com
SMTP_PASS=your-password
SMTP_FROM=your-email@yourdomain.com
