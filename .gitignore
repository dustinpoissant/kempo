node_modules
emails
.vscode
.env
.env.production
*.log
test-smtp*.js
vps-*.js
vps-*.sh
vps-*.ps1
emails/
tasks/

# Written at runtime by createAdminGlobalContent when an extension installs its admin nav.
# Per-installation state, not source. Note: `npm run build` wipes dist/ and destroys it.
dist/admin/kempo-global.global.html
